#include<iostream>
//#include "stdafx.h"
#include<stdlib.h>
using namespace std;
int main(){
    int *A,n,ch,x;
    printf("Enter dimension");
    scanf("%d",&n);
    A = (int*) malloc(n*sizeof(int));
    do
    {
        switch (expression)
        {
        case /* constant-expression */:
            /* code */
            break;
        
        default:
            break;
        }
    } while (/* condition */);
    
return 0;
}